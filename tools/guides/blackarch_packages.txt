# CHECKSEC for checking the properties and security settings of binaries
$ sudo pacman -S checksec
$ checksec --version


# EXIFTOOL for checking metadata of a file
# Update the package list:
$ sudo pacman -Sy
# Install exiftool:
$ sudo pacman -S perl-image-exiftool
# Try running the following command to verify the installation and check the version:
$ exiftool -ver
# Check if exiftool is installed:
$ pacman -Ql perl-image-exiftool | grep bin
# Locate exiftool:
$ which exiftool
# You can run exiftool using the full path:
$ /usr/bin/vendor_perl/exiftool -ver
# Create a symbolic link to make exiftool accessible from anywhere:
$ sudo ln -s /usr/bin/vendor_perl/exiftool /usr/local/bin/exiftool
# After creating the symbolic link, you can verify the installation again:
$ exiftool -ver


# JOHN THE RIPPER for cracking passwords
$ sudo pacman -S john


# NETCAT for connecting using "nc"
$ sudo pacman -S netcat
$ nc -h


# WHICH for finding files
$ sudo pacman -S which


+---------------------------+
+       Languages           +
+---------------------------+
# JAVA
$ sudo pacman -Syyu
# search JDK packages
$ sudo pacman -Ss jdk
# Choose the version you want to install. For example, to install OpenJDK 17 but preferably the latest, run:
$ sudo pacman -S jdk17-openjdk
# To set the JAVA_HOME environment variable, you can add the following lines to your ~/.bashrc or ~/.zshrc file:
$ export JAVA_HOME=/usr/lib/jvm/java-17-openjdk
$ export PATH=$PATH:$JAVA_HOME/bin
# After editing the profile file, source it to apply the changes:
$ source ~/.bashrc
$ java -version

# C++/C
$ sudo pacman -Syu
$ sudo pacman -S gcc
$ gcc --version
# install other development tools
$ sudo pacman -S make
$ sudo pacman -S gdb
$ sudo pacman -S base-devel

+-----------------------------------+
+       Web Exploitation            +
+-----------------------------------+
# Burb Suite Community
https://portswigger.net/burp/releases/professional-community-2024-5-5?requestededition=community&requestedplatform=